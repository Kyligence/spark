apiVersion: v1
kind: Service
metadata:
  name: {{ include "nacos-frontend.fullname" . }}
  labels:
    {{- include "nacos-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nacos-frontend.selectorLabels" . | nindent 4 }}
