apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kyligence-enterprise.fullname" . }}-test-connection"
  labels:
    {{- include "kyligence-enterprise.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kyligence-enterprise.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
