{{- if .Values.naming.ingress.enabled -}}
{{- $fullName := printf "%s-naming" (include "nacos.fullname" .) -}}
{{- $releaseName := .Release.Name -}}
{{- $serviceName := printf "%s-naming-svc" (include "nacos.fullname" .) -}}
{{- $svcPort := .Values.naming.service.port -}}
{{- if and .Values.naming.ingress.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.naming.ingress.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.naming.ingress.annotations "kubernetes.io/ingress.class" .Values.naming.ingress.className}}
  {{- end }}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}
  labels:
    {{- include "nacos.labels" . | nindent 4 }}
  {{- with .Values.naming.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.naming.ingress.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.naming.ingress.className }}
  {{- end }}
  {{- if .Values.naming.ingress.tls }}
  tls:
    {{- range .Values.naming.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.naming.ingress.hosts }}
    - host: {{ printf "%s.%s" $releaseName .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $serviceName }}
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $serviceName }}
              servicePort: {{ $svcPort }}
              {{- end }}
          {{- end }}
    {{- end }}
{{- end }}
