apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nacos-frontend.fullname" . }}-sitemap-cm
data:
  sitemap.json: |-
    {
        "IAM_instance": "{{ .Values.iam.serverAddr }}",
        "Configuration_instance": "{{ .Values.config.serverAddr }}"
    }