apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nacos.fullname" .}}-config-cm
data:
  MYSQL_SERVICE_HOST: {{ .Values.mysql.host | default .Values.config.mysql.host }}
  MYSQL_SERVICE_DB_NAME: {{ .Values.mysql.db }}
  MYSQL_SERVICE_PORT: "{{ .Values.mysql.port  | default .Values.config.mysql.port }}"