apiVersion: v1
kind: Service
metadata:
  name: {{ include "yl-log.fullname" . }}-api-service
  labels:
  {{- include "yl-log.labels" . | nindent 4 }}
spec:
  type: {{ .Values.apiService.type }}
  selector:
    app: log
    component: api
  {{- include "yl-log.selectorLabels" . | nindent 4 }}
  ports:
	{{- .Values.apiService.ports | toYaml | nindent 2 -}}