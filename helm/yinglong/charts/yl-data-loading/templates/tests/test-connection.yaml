apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "yl-data-loading.fullname" . }}-test-connection"
  labels:
    {{- include "yl-data-loading.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "yl-data-loading.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
