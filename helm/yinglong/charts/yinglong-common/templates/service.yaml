apiVersion: v1
kind: Service
metadata:
  name: {{ include "yinglong-common.fullname" . }}
  labels:
    {{- include "yinglong-common.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.debugPort }}
      targetPort: debug
      protocol: TCP
      name: debug
  selector:
    {{- include "yinglong-common.selectorLabels" . | nindent 4 }}
