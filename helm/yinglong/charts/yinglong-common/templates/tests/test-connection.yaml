apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "yinglong-common.fullname" . }}-test-connection"
  labels:
    {{- include "yinglong-common.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "yinglong-common.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
