apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ .Values.sparkRole }}-{{ .Release.Namespace }}
rules:
  - verbs:
      - '*'
    apiGroups:
      - ''
    resources:
      - '*'