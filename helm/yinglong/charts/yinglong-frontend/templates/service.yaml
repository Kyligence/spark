apiVersion: v1
kind: Service
metadata:
  name: {{ include "yinglong-frontend.fullname" . }}
  labels:
    {{- include "yinglong-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "yinglong-frontend.selectorLabels" . | nindent 4 }}
