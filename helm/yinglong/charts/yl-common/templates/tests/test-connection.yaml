apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "yl-common.fullname" . }}-test-connection"
  labels:
    {{- include "yl-common.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "yl-common.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
