apiVersion: v1
kind: Service
metadata:
  name: {{ include "yl-gateway.fullname" . }}
  labels:
    {{- include "yl-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "yl-gateway.selectorLabels" . | nindent 4 }}
