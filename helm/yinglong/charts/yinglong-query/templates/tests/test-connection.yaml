apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "yinglong-query.fullname" . }}-test-connection"
  labels:
    {{- include "yinglong-query.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "yinglong-query.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
