apiVersion: v1
kind: Service
metadata:
  name: {{ include "yinglong-commmon.fullname" . }}
  labels:
    {{- include "yinglong-commmon.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.debugPort }}
      targetPort: debug
      protocol: TCP
      name: debug
  selector:
    {{- include "yinglong-commmon.selectorLabels" . | nindent 4 }}
