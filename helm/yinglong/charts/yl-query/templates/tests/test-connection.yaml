apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "yl-query.fullname" . }}-test-connection"
  labels:
    {{- include "yl-query.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "yl-query.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
