apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "yl-ops.fullname" . }}-test-connection"
  labels:
    {{- include "yl-ops.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "yl-ops.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
