# Default values for yinglong.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

global:
  hadoopPrefix: hadoop
  hdfs: {}
    # workingDir: 

  image:
    tag: "latest"
    pullPolicy: IfNotPresent

  metadata:
    url: "*****" # yinglong所在的mysql的URL地址, (用户名和密码在参数上)
  nacos:
    config:
      serverAddrNoHttp: nacos-config-svc.yinglong:8848
    discovery:
      serverAddrNoHttp: nacos-naming-svc.yinglong:8848
    # serverAddr:
  
  tenantId: "" # 部署一个yinglong-release需要的租户ID
  publicGroup: ""   # 租户中心在nacos-naming中的组

  zookeeper: {}
    # address: localhost:2181
  tz: "Asia/Shanghai"        # 部署yinglong的时区, 必须考虑实际情况.

  configcenter:
    disabled: false         # 使用非配置中心版本的yinglong (beta) 为xuanwu-offering 10月演示特殊设置.

  spark:
    image:
      repository: harbor-ofs.kyligence.com/yinglong/yinglong-spark
      tag: latest

# yl-common:
#   image:
#     repository: harbor-ofs.kyligence.com/yinglong/yl-common
#     tag: yl-5.0.10
#   fullnameOverride: yl-common-with-a-very-long-name

# yl-data-loading:
#   image:
#     repository: harbor-ofs.kyligence.com/yinglong/yl-data-loading
#     tag: yl-5.0.10
#   fullnameOverride: yl-data-loading-with-a-very-long-name

# yl-query:
#   image:
#     repository: harbor-ofs.kyligence.com/yinglong/yl-query
#     tag: yl-5.0.10
#   fullnameOverride: yl-query-with-a-very-long-name

# yl-smart:
#   image:
#     repository: harbor-ofs.kyligence.com/yinglong/yl-smart
#     tag: yl-5.0.10

# yl-resource:
#   image:
#     repository: repo-ofs.kyligence.com/kyligence/yl-resource
#     tag: yl-5.0.10

# yl-ops:
#   image:
#     repository: harbor-ofs.kyligence.com/yinglong/yl-ops
#     tag: yl-5.0.10   

# yl-frontend:
#   image:
#     repository: harbor-ofs.kyligence.com/yinglong/yl-frontend
#     tag: yl-5.0.10
#   gateway:
#     serverAddr: gateway:8080
#   iam:
#     serverAddr: https://iam.dev.kyligence.com/   # 租户中心的访问地址.
#   config:
#     serverAddr: https://nacos.dev.kyligence.com/   # 配置中心的访问地址.
#   ingress:
#     hosts:
#       - host: dev.kyligence.com  # yinglong 最终的访问地址是 https://{helm-release-name}.{k8s-namespace}.{host}/
#         paths:
#           - path: /
#             pathType: ImplementationSpecific

# yl-gateway:
#   image:
#     repository: repo-ofs.kyligence.com/gateway
#     tag: "5.0.1-private"
#   fullnameOverride: gateway
#   config: # redis参数必须和租户中心的gateway所使用的redis一致.
#     redis:
#       host: redis-master.yinglong.svc.cluster.local
#       database: 6
#       password: "******"
