# Default values for yinglong.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

global:
  hadoopPrefix: hadoop
  hdfs: {}
    # workingDir: 

  metadata:
    user: root
    password: kyligence
    database: kylin
    # url: ke_metadata@jdbc,driverClassName=org.postgresql.Driver,url=jdbc:postgresql://localhost:5432/kylin,username=postgres,password=
    identifierPrefix: ke_metadata_yinglong
  nacos:
    config:
      serverAddr: http://nacos-config-svc.yinglong:8848
      serverAddrNoHttp: nacos-config-svc.yinglong:8848
    discovery:
      serverAddr: http://nacos-naming-svc.yinglong:8848
    # serverAddr:
  
  tenantId: "54642236552822785"
  publicGroup: "yinglong"

  zookeeper: {}
    # address: localhost:2181
