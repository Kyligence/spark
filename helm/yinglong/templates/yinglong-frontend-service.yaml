apiVersion: v1
kind: Service
metadata:
  name: yinglong-frontend-service-{{ .Release.Name }}
spec:
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: yinglong-frontend-{{ .Release.Name }}
  type: ClusterIP
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800