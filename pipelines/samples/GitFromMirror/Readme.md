# 为什么需要这个方法？

> Github服务是墙外的一个服务，如果要访问收到很多影响，在以往的经验里，拉去一次全量代码非常的耗时，而且稳定性很差，为了解决这个问题，我们选择了 git mirror 的方式。

### 具体的使用流程：

1. Jira中申请devops工单，将你的github仓库添加到git mirror服务中；
2. 获取代码是，需要调用 vars/pullCodeFromGitMirror 方法，具体用法可已阅读参数说明，也可以参考示例代码。