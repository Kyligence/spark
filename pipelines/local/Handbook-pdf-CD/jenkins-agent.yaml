---
kind: "Pod"
name: "handbook-agent"
spec:
  containers:
    - name: "git"
      image: "bitnami/git:2.35.1-debian-10-r68"
      volumeMounts:
        - mountPath: "/var/cicd"
          name: "jenkins-common"
          subPath: "newten-cicd"
      tty: true
      command:
        - "cat"
      resources:
        limits:
          memory: "1Gi"
          cpu: "1000m"
    - name: "build"
      image: "repo-ofs.kyligence.tech:5000/handbook:1.11"
      imagePullPolicy: "IfNotPresent"
      tty: true
      command:
        - "cat"
      resources:
        limits:
          memory: "8Gi"
          cpu: "4000m"
      volumeMounts:
        - mountPath: "/gitbook"
          name: "jenkins-common"
          subPath: "handbook"
        - mountPath: "/var/cicd"
          name: "jenkins-common"
          subPath: "newten-cicd"
        - mountPath: "/etc/pem/rsa.pem"
          name: "handbook-secret"
          subPath: "rsa.pem"
  volumes:
    - name: "jenkins-common"
      persistentVolumeClaim:
        claimName: "jenkins-common"
    - name: "handbook-secret"
      secret:
        secretName: "handbook-secret"
        defaultMode: 0400
