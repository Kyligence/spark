apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-claim
  namespace: ops
spec:
  storageClassName: nfs-dynamic
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Mi