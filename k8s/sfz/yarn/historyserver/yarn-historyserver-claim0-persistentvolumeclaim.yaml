apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: yarn-historyserver-claim0
  namespace: <<<namespace>>>
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: local-path