apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jenkins-common 
  namespace: cicd
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: efs-sc
  resources:
    requests:
      storage: 300Gi
