kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: nexus-test-pvc
  namespace: cicd
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2Ti
  storageClassName: ""
  volumeName: nexus-test-pv
  volumeMode: Filesystem
