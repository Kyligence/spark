kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: nexus-storage-pvc
  namespace: cicd
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2Ti
  storageClassName: ""
  volumeName: nexus-storage
  volumeMode: Filesystem
